{
  "name": "vasisotirova-admin-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Upload service backing the gallery admin page",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
